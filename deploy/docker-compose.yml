services:
  odoo-mcp:
    build:
      context: ../odoo_mcp_server
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${ODOO_MCP_BIND:-0.0.0.0}:8088:8088"
    volumes:
      - ./audit:/var/log/odoo-mcp
